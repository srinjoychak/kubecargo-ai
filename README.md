# kubecargo-ai
kubecargo-ai is an AI-powered tool that generates complete offline Kubernetes bundles for air-gapped deployments. Given a K8s version and OS, it auto-resolves dependencies and packages binaries, images, and system packages into ready-to-use artifacts.
